<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Tarea</title>
</head>
<body>
  <h1>Editar Tarea</h1>
  <form action="/tareas/<%= tarea._id %>?_method=PUT" method="POST">
    <label>Título:
      <input type="text" name="titulo" value="<%= tarea.titulo %>" required>
    </label><br>

    <label>Descripción:
      <input type="text" name="descripcion" value="<%= tarea.descripcion %>" required>
    </label><br>

    <label>Estado:
      <select name="estado" required>
        <option value="pendiente" <%= tarea.estado==='pendiente' ? 'selected' : '' %>>Pendiente</option>
        <option value="en_progreso" <%= tarea.estado==='en_progreso' ? 'selected' : '' %>>En progreso</option>
        <option value="completado" <%= tarea.estado==='completado' ? 'selected' : '' %>>Completado</option>
      </select>
    </label><br>

    <label>Fecha de creación:
      <input type="date" name="fecha_creacion"
             value="<%= new Date(tarea.fecha_creacion).toISOString().slice(0,10) %>">
    </label><br>

    <button type="submit">Actualizar</button>
  </form>
  <a href="/">Regresar</a>
</body>
</html>
